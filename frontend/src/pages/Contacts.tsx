import { SupportForm } from '../components/SupportForm';

export const Contacts = () => {
   return (
      <section className="mx-auto max-w-7xl px-4 py-10">
         <h1 className="text-[40px] font-bold pb-10">Контакты</h1>
         <SupportForm />
      </section>
   );
};
